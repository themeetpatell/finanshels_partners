import { Linkedin } from 'lucide-react'
import { Card } from './ui/Card'

// This component is now directly integrated in Home.jsx for better layout control
export default function LeadershipCard({ leader }) {
  return null
}

